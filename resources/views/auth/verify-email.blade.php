<x-guest-layout>
    <div class="mb-4 text-sm text-gray-600">
        {{ __('¡Estas a un paso! Antes de empezar a ojear ofertas, revisa tu email para verificar la cuenta. Si al cabo de unos minutos no lo recibes, puedes solicitar otro. Nos vemos al otro lado ;)') }}
    </div>

    @if (session('status') == 'verification-link-sent')
        <div class="mb-4 font-medium text-sm text-green-600">
            {{ __('Un nuevo correo ha sido enviado a tu email proporcionado en el registro.') }}
        </div>
    @endif

    <div class="mt-4 flex items-center justify-between">
        <form method="POST" action="{{ route('verification.send') }}">
            @csrf

            <div>
                <x-primary-button>
                    {{ __('Enviar nueva verificación') }}
                </x-primary-button>
            </div>
        </form>

        <form method="POST" action="{{ route('logout') }}">
            @csrf

            <button type="submit" class=" text-sm text-gray-600 hover:text-gray-900 rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                {{ __('Cerrar sesión') }}
            </button>
        </form>
    </div>
</x-guest-layout>
